{
  "name": "AWSCodeDeployDeploymentGroupOnPremisesInstanceTagFilters",
  "description": "OnPremisesInstanceTagFilters is a property of the AWS::CodeDeploy::DeploymentGroup resource that specifies which on-premises instances to associate with the deployment group. To register on-premise instances with AWS CodeDeploy, see Configure Existing On-Premises Instances by Using AWS CodeDeploy in the AWS CodeDeploy User Guide.",
  "properties": [
    {
      "name": "Key",
      "description": "Filter on-premises instances with this key.",
      "required": false,
      "type": "String"
    },
    {
      "name": "Type",
      "description": "The filter type. For example, you can filter on-premises instances by the key, tag value, or both. For valid values, see EC2TagFilter in the AWS CodeDeploy API Reference.",
      "required": false,
      "type": "String"
    },
    {
      "name": "Value",
      "description": "Filter on-premises instances with this tag value.",
      "required": false,
      "type": "String"
    }
  ]
}