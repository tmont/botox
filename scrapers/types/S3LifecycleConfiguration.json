{
  "name": "S3LifecycleConfiguration",
  "description": "Describes the lifecycle configuration for objects in an AWS::S3::Bucket resource.",
  "properties": [
    {
      "name": "Rules",
      "description": "A lifecycle rule for individual objects in an S3 bucket.",
      "required": true,
      "type": "S3LifecycleRule"
    }
  ]
}