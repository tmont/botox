{
  "name": "ElasticMapReduceClusterBootstrapActionConfigScriptBootstrapActionConfig",
  "description": "ScriptBootstrapActionConfig is a property of the Amazon Elastic MapReduce Cluster BootstrapActionConfig property that specifies the arguments and location of the bootstrap script that Amazon Elastic MapReduce (Amazon EMR) runs before it installs applications on the cluster nodes.",
  "properties": [
    {
      "name": "Args",
      "description": "A list of command line arguments to pass to the bootstrap action script.",
      "required": false,
      "type": "string[]"
    },
    {
      "name": "Path",
      "description": "The location of the script that Amazon EMR runs during a bootstrap action. Specify a location in an S3 bucket or your local file system.",
      "required": true,
      "type": "String"
    }
  ]
}