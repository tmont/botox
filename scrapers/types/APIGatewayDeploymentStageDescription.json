{
  "name": "APIGatewayDeploymentStageDescription",
  "description": "StageKey is a property of the AWS::ApiGateway::Deployment resource that configures an Amazon API Gateway (API Gateway) deployment stage.",
  "properties": [
    {
      "name": "CacheClusterEnabled",
      "description": "Indicates whether cache clustering is enabled for the stage.",
      "required": false,
      "type": "Boolean"
    },
    {
      "name": "CacheClusterSize",
      "description": "The size of the stage's cache cluster.",
      "required": false,
      "type": "String"
    },
    {
      "name": "ClientCertificateId",
      "description": "The identifier of the client certificate that API Gateway uses to call your integration endpoints in the stage.",
      "required": false,
      "type": "String"
    },
    {
      "name": "DeploymentId",
      "description": "The ID of the deployment that the stage points to.",
      "required": false,
      "type": "String"
    },
    {
      "name": "Description",
      "description": "A description of the purpose of the stage.",
      "required": false,
      "type": "String"
    },
    {
      "name": "MethodSettings",
      "description": "Configures settings for all of the stage's methods.",
      "required": false,
      "type": "APIGatewayDeploymentStageDescriptionMethodSetting"
    },
    {
      "name": "RestApiId",
      "description": "The ID of a RestApi resource that you're deploying with this stage.",
      "required": false,
      "type": "String"
    },
    {
      "name": "StageName",
      "description": "The name of the stage, which API Gateway uses as the first path segment in the invoke Uniform Resource Identifier (URI).",
      "required": false,
      "type": "String"
    },
    {
      "name": "Variables",
      "description": "A map that defines the stage variables. Variable names must consist of alphanumeric characters, and the values must match the following regular expression: [A-Za-z0-9-._~:/?#&amp;=,]+.",
      "required": false,
      "type": "key[]"
    }
  ],
  "referenceUrl": "http://docs.aws.amazon.com/AWSCloudFormation/latest/UserGuide/aws-properties-apitgateway-deployment-stagedescription.html"
}