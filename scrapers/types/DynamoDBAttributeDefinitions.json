{
  "name": "DynamoDBAttributeDefinitions",
  "description": "A list of attribute definitions for the AWS::DynamoDB::Table resource. Each element is composed of an AttributeName and AttributeType.",
  "properties": [
    {
      "name": "AttributeName",
      "description": "The name of an attribute. Attribute names can be 1 – 255 characters long and have no character restrictions.",
      "required": true,
      "type": "String"
    },
    {
      "name": "AttributeType",
      "description": "The data type for the attribute. You can specify S for string data, N for numeric data, or B for binary data.",
      "required": true,
      "type": "String"
    }
  ]
}