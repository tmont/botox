{
  "name": "DirectoryServiceMicrosoftADVpcSettings",
  "description": "VpcSettings is a property of the AWS::DirectoryService::MicrosoftAD resource that specifies the VPC settings for a Microsoft directory server.",
  "properties": [
    {
      "name": "SubnetIds",
      "description": "A list of two subnet IDs for the directory servers. Each subnet must be in different Availability Zones (AZs). AWS Directory Service creates a directory server and a DNS server in each subnet.",
      "typeDescription": "List of strings",
      "required": true,
      "type": "string[]"
    },
    {
      "name": "VpcId",
      "description": "The VPC ID in which to create the Microsoft Active Directory server.",
      "typeDescription": "String",
      "required": true,
      "type": "String"
    }
  ],
  "referenceUrl": "http://docs.aws.amazon.com/AWSCloudFormation/latest/UserGuide/aws-properties-directoryservice-microsoftad-vpcsettings.html"
}