{
  "fullName": "AWS::OpsWorks::App",
  "namespace": "AWS",
  "category": "OpsWorks",
  "name": "App",
  "shortDesc": "Defines an AWS OpsWorks app for an AWS OpsWorks stack. The app represents code that you want to run on an application server.",
  "properties": [
    {
      "name": "AppSource",
      "desc": "Contains the information required to retrieve an app from a repository.",
      "required": false,
      "type": "AWS OpsWorks Source Type",
      "update": "No interruption"
    },
    {
      "name": "Attributes",
      "desc": "One or more user-defined key-value pairs to be added to the stack attributes bag.",
      "required": false,
      "type": "A list of key-value pairs",
      "update": "No interruption"
    },
    {
      "name": "Description",
      "desc": "A description of the app.",
      "required": false,
      "type": "String",
      "update": "No interruption"
    },
    {
      "name": "Domains",
      "desc": "The app virtual host settings, with multiple domains separated by commas. For example, 'www.example.com, example.com'.",
      "required": false,
      "type": "List of strings",
      "update": "No interruption"
    },
    {
      "name": "EnableSsl",
      "desc": "Whether to enable SSL for this app.",
      "required": false,
      "type": "Boolean",
      "update": "No interruption"
    },
    {
      "name": "Environment",
      "desc": "The environment variables to associate with the AWS OpsWorks app.",
      "required": false,
      "type": "List of AWS OpsWorks App Environment",
      "update": "No interruption"
    },
    {
      "name": "Name",
      "desc": "The AWS OpsWorks app name.",
      "required": true,
      "type": "String",
      "update": "No interruption"
    },
    {
      "name": "Shortname",
      "desc": "The app short name, which is used internally by AWS OpsWorks and by Chef recipes.",
      "required": false,
      "type": "String",
      "update": "Replacement"
    },
    {
      "name": "SslConfiguration",
      "desc": "The SSL configuration",
      "required": false,
      "type": "AWS OpsWorks SslConfiguration Type",
      "update": "No interruption"
    },
    {
      "name": "StackId",
      "desc": "The AWS OpsWorks stack ID that this app will be associated with.",
      "required": true,
      "type": "String",
      "update": "Replacement"
    },
    {
      "name": "Type",
      "desc": "The app type. Each supported type is associated with a particular layer. For more information, see CreateApp in the AWS OpsWorks API Reference.",
      "required": true,
      "type": "String",
      "update": "No interruption"
    }
  ],
  "attributes": []
}