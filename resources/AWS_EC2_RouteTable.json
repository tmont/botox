{
  "fullName": "AWS::EC2::RouteTable",
  "namespace": "AWS",
  "category": "EC2",
  "name": "RouteTable",
  "shortDesc": "Creates a new route table within a VPC. After you create a new route table, you can add routes and associate the table with a subnet.",
  "properties": [
    {
      "name": "VpcId",
      "desc": "The ID of the VPC where the route table will be created.",
      "required": true,
      "type": "String",
      "update": "Replacement"
    },
    {
      "name": "Tags",
      "desc": "An arbitrary set of tags (key–value pairs) for this route table.",
      "required": false,
      "type": "AWS CloudFormation Resource Tags",
      "update": "No interruption."
    }
  ],
  "attributes": []
}