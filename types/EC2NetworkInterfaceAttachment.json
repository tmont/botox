{
  "name": "EC2NetworkInterfaceAttachment",
  "description": "Describes a network interface attachment for an Elastic Network Interface (ENI). AWS::EC2::NetworkInterface takes an object of this type in its Attachment property.",
  "properties": [
    {
      "name": "AttachmentID",
      "desc": "The ID of the network interface attachment.",
      "required": true,
      "type": "String"
    },
    {
      "name": "InstanceID",
      "desc": "The ID of the instance attached to the network interface.",
      "required": true,
      "type": "String"
    }
  ]
}