{
  "name": "EC2ContainerServiceTaskDefinitionVolumesHost",
  "description": "Host is a property of the Amazon EC2 Container Service TaskDefinition Volumes property that specifies the data volume path on the host container instance.",
  "properties": [
    {
      "name": "SourcePath",
      "desc": "The data volume path on the host container instance.",
      "required": false,
      "type": "String"
    }
  ]
}