{
  "name": "AWSDataPipelinePipelinePipelineTags",
  "description": "PipelineTags is a property of the AWS::DataPipeline::Pipeline resource that defines arbitrary key-value pairs for a pipeline.",
  "properties": [
    {
      "name": "Key",
      "desc": "The key name of a tag.",
      "required": true,
      "type": "String"
    },
    {
      "name": "Value",
      "desc": "The value to associate with the key name.",
      "required": true,
      "type": "String"
    }
  ]
}