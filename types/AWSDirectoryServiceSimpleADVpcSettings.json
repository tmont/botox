{
  "name": "AWSDirectoryServiceSimpleADVpcSettings",
  "description": "VpcSettings is a property of the AWS::DirectoryService::SimpleAD resource that specifies the VPC settings for a directory server.",
  "properties": [
    {
      "name": "SubnetIds",
      "desc": "A list of two subnet IDs for the directory servers. Each subnet must be in different Availability Zones (AZ). AWS Directory Service creates a directory server and a DNS server in each subnet.",
      "required": true,
      "type": "List of strings"
    },
    {
      "name": "VpcId",
      "desc": "The VPC ID in which to create the Simple AD directory.",
      "required": true,
      "type": "String"
    }
  ]
}