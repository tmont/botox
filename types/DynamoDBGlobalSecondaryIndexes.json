{
  "name": "DynamoDBGlobalSecondaryIndexes",
  "description": "Describes global secondary indexes for the AWS::DynamoDB::Table resource.",
  "properties": [
    {
      "name": "IndexName",
      "desc": "The name of the global secondary index. The index name can be 3 – 255 characters long and have no character restrictions.",
      "required": true,
      "type": "String"
    },
    {
      "name": "KeySchema",
      "desc": "The complete index key schema for the global secondary index, which consists of one or more pairs of attribute names and key types.",
      "required": true,
      "type": "DynamoDB Key Schema"
    },
    {
      "name": "Projection",
      "desc": "Attributes that are copied (projected) from the source table into the index. These attributes are in addition to the primary key attributes and index key attributes, which are automatically projected.",
      "required": true,
      "type": "DynamoDB Projection Object"
    },
    {
      "name": "ProvisionedThroughput",
      "desc": "The provisioned throughput settings for the index.",
      "required": true,
      "type": "DynamoDB Provisioned Throughput"
    }
  ]
}