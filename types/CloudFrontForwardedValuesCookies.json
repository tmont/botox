{
  "name": "CloudFrontForwardedValuesCookies",
  "description": "Cookies is a property of the CloudFront ForwardedValues that describes which cookies are forwarded to the Amazon CloudFront origin.",
  "properties": [
    {
      "name": "Forward",
      "desc": "The cookies to forward to the origin of the cache behavior. You can specify none, all, or whitelist.",
      "required": true,
      "type": "String"
    },
    {
      "name": "WhitelistedNames",
      "desc": "The names of cookies to forward to the origin for the cache behavior.",
      "required": false,
      "type": "List of strings"
    }
  ]
}