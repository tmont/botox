{
  "name": "CloudFrontDistributionConfigurationViewerCertificate",
  "description": "ViewerCertificate is a property of the CloudFront DistributionConfig property that specifies which certificate to use when viewers use HTTPS to request objects.",
  "properties": [
    {
      "name": "CloudFrontDefaultCertificate",
      "desc": "Indicates whether to use the default certificate for your CloudFront domain name when viewers use HTTPS to request your content.",
      "required": false,
      "type": "Boolean"
    },
    {
      "name": "IamCertificateId",
      "desc": "The IAM certificate ID to use if you're using an alternate domain name.",
      "required": false,
      "type": "String"
    },
    {
      "name": "MinimumProtocolVersion",
      "desc": "The minimum version of the SSL protocol that you want CloudFront to use for HTTPS connections. CloudFront serves your objects only to browsers or devices that support at least the SSL version that you specify.",
      "required": false,
      "type": "String"
    },
    {
      "name": "SslSupportMethod",
      "desc": "Specifies how CloudFront serves HTTPS requests.",
      "required": false,
      "type": "String"
    }
  ]
}