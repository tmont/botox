{
  "name": "AWSCodePipelinePipelineStagesBlockers",
  "description": "Blockers is a property of the AWS CodePipeline Pipeline Stages property that specifies an AWS CodePipeline gate declaration.",
  "properties": [
    {
      "name": "Name",
      "desc": "The name of the gate declaration.",
      "required": true,
      "type": "String"
    },
    {
      "name": "Type",
      "desc": "The type of gate declaration. For valid values, see BlockerDeclaration in the AWS CodePipeline API Reference.",
      "required": true,
      "type": "String"
    }
  ]
}